import { MainDashboardLayout } from "@/features/dashboard/screens/DashboardLayout";

export default function DashboardLayout(props: React.PropsWithChildren) {
  return <MainDashboardLayout {...props} />;
}
